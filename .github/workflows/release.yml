name: Android Release

on:
  push:
    tags:
      - "*.*.*"

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK (arm64)
        env:
          PRIVATELM_ALLOW_DEBUG_RELEASE_SIGNING: "true"
          GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Dfile.encoding=utf-8 -Dkotlin.daemon.jvm.options=-Xmx4g"
        run: flutter build apk --release --target-platform android-arm64

      - name: Build APK (armeabi-v7a)
        env:
          PRIVATELM_ALLOW_DEBUG_RELEASE_SIGNING: "true"
          GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Dfile.encoding=utf-8 -Dkotlin.daemon.jvm.options=-Xmx4g"
        run: flutter build apk --release --target-platform android-arm-v7a

      - name: Upload APK artifacts (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: privatelm-apk-arm64-${{ github.ref_name }}
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk

      - name: Upload APK artifacts (armeabi-v7a)
        uses: actions/upload-artifact@v4
        with:
          name: privatelm-apk-armeabi-${{ github.ref_name }}
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
